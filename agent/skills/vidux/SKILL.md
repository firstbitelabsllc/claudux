---
name: vidux
description: Resume durable Vidux-style plan rows and leave cloneable handoff evidence for Claudux receiver work.
---

# Vidux Discipline

Use durable files over chat memory. Claudux has no local `PLAN.md` at this
receiver start; repo-owned proof lives under `evidence/`, and cross-project
command-center receipts live under
`/Users/leokwan/Development/vidux/projects/agentic-command-center/`.

When adding receiver proof:

- Record proof commands, branch, worktree, commit, PR URL, and non-claims.
- Keep generated docs out of receiver edits unless a source-owned docs task
  explicitly requires regeneration.
- Leave the next agent a cloneable resume path.
