name: Blacksmith Testbox
on:
  workflow_dispatch:
    inputs:
      testbox_id:
        type: string
        description: "Testbox session ID"
        required: true
  pull_request:
    paths:
      - '.github/workflows/**'

jobs:
  local-ci-pointer:
    name: Local CI pointer
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 2
    permissions:
      contents: read
    steps:
      - name: Begin Testbox
        uses: useblacksmith/begin-testbox@v2
        with:
          testbox_id: ${{ inputs.testbox_id }}
      - name: Point to local proof
        run: |
          echo "GitHub Actions CI is disabled for this repo."
          echo "Use local proof commands from the repo contract, then cite the results."
          echo "Common proof commands here:"
          echo "  bash tests/run-tests.sh"
          echo "  bash tests/run-all.sh"
      - name: Run Testbox
        uses: useblacksmith/run-testbox@v2
        if: always()
