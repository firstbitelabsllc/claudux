# claudux — CODE RED: launch the drift-gate re-center

**Status:** ACTIVE · **Authority:** this file is the sole mission authority · **Repo:** `firstbitelabsllc/claudux` (canonical public) · **Trunk:** `origin/main`

## Mission (one line)

Drive claudux from "re-centered + drift gate landed on `origin/main` (#85, `2d1f872`)" to "the drift gate is dogfooded in claudux's OWN CI, proven on a second real repo, and 1.2.0 is published" — ranking real launch-credibility risk first, landing by the repo's normal branch/PR rule, never spinning on a human gate.

## What is already TRUE on origin/main (grounded 2026-07-19, ref `origin/main` = `2d1f872`)

- `lib/drift.sh` — deterministic comparator, wired: `REQUIRED_LIBS` includes `drift.sh`; `"drift")` case in `bin/claudux:375` calls `claudux_drift`.
- `docs-drift-lock.json` — committed baseline (`baseline=269cafb`), un-ignored.
- `tests/test-drift.sh` — drift test suite (auto-discovered by `tests/run-all.sh`).
- `.github/workflows/docs-drift.yml` — copy-paste CI job.
- README re-centered: hook "Your build fails the moment a doc starts lying." + drift-verdict banner + terminal-demo SVG + comparison table with staleness row + honest install (source 1.2.0 vs npm 1.1.1).
- GitHub repo "About" description + topics re-centered to the drift positioning (fixed 2026-07-19).
- **Proven live this session** (detached worktree off `origin/main`): `claudux drift` clean → exit 0; surface change to a documented `lib/*.sh` with doc untouched → exit 1, names 6 doc pages + the changed source + the exact fix; restore → exit 0; `--json` emits `{command,sensitivity,baseline,head_sha,ready,drifted[],new_sources[]}`.

## Setup Contract (readiness before deep work)

- **No credentials needed for the core mission** — the gate is keyless, offline, deterministic. This is the entire pitch; never add a network/API dependency to the pass/fail path.
- `npm publish` needs `npm login` (Leo's token) — LEO-GATED, see below.
- Second-repo dogfood needs a target repo already on this machine (e.g. `resplit-ios/.claudux-tool`, `resplit-web`, `strongyes-web`) — no new access required.
- Browser proof uses headless Playwright CLI (`PLAYWRIGHT_BROWSERS_PATH=~/Library/Caches/ms-playwright`) — already available; claude-in-chrome extension is NOT connected, so do not wait on it.

## P0-first inference (rank by launch-credibility blast radius, not easy-close)

The largest reachable risk to this launch is **"is it actually a real lint or just an AI wrapper?"** Everything that hardens that answer ranks above polish.

### Rows (agent-completable)

| # | Row | Priority | validation |
|---|-----|----------|------------|
| R1 | **Dogfood the gate in claudux's own CI.** `docs-drift.yml` currently ships as an *example*; make the repo actually run `claudux drift` on its own PRs (or confirm it already gates and record the run URL). The credibility money-shot: claudux's own red X on a drifting PR. | P0 | CI run URL showing `claudux drift` executing on a PR; a deliberate drift PR going red, then green after `--accept`/doc-fix. |
| R2 | **Re-baseline the lock to include `lib/drift.sh`.** Current baseline `269cafb` predates `drift.sh`, so every clean run prints a "new source (no baseline)" note for it. Run `claudux drift --accept`, commit the updated `docs-drift-lock.json`, confirm the note clears. | P0 | `claudux drift` exits 0 with an EMPTY `new_sources[]`; committed lock diff is timestamp-free and reviewable. |
| R3 | **Prove the gate on a second REAL repo** (not a fixture). Pick one owned repo on-disk, run `claudux drift --accept` to seed a lock, make a real code+no-doc change, confirm exit 1 names the right doc, restore. Record the transcript. | P0 | exit-code transcript from a non-claudux repo naming the correct doc/source pair. |
| R4 | **Full green gate on `origin/main` HEAD**: `shellcheck`, `tests/run-all.sh`, `tests/test-drift.sh`, `claudux audit --release` (if present), `npm pack --dry-run`. Fix any red. | P0 | pasted pass output of each; any fix lands via branch/PR. |
| R5 | **Wire `--warn-only` pre-commit example + README "Add to CI" polish** only after R1–R4 are green. Verify every doc snippet (CI YAML, pre-commit one-liner) actually runs as written. | P2 | copy-run each snippet; no drift between doc and behavior. |
| R6 | **Browser-render proof**: headless-capture the live GitHub README body and confirm the drift-verdict banner, terminal-demo SVG, and comparison table render (light+dark). | P2 | screenshots read back; note any SVG that fails to render on GitHub's `<img>` sanitizer. |

**P0 inference line (emit every cycle before editing):**
`P0 inference: candidates=[...] selected=<row> because=<largest reachable credibility break> deferred=<why the rest wait>`

Ladder discipline: a green row stopped at `branch_pushed`/`pr_open`/`merged` moves UP the ladder before any new P2 work.

## LEO-GATED handoffs (NOT agent-completable — park as handoffs, never a whole-goal stop)

- **`npm publish` 1.2.0** — needs `npm login` (Leo's token). npm README freezes per version; the re-centered README is already on `main`, so publish is unblocked the moment Leo runs login. Agent prepares `npm pack --dry-run` + a one-command publish recipe; Leo runs it.
- **External posting** (blog / LinkedIn / X) about the re-center — Leo's voice, Leo's call.
- **Restoring the retracted leojkwan.com post / reopening claudux #82** — stay as-is unless Leo says otherwise.

When a cycle finds only LEO-GATED work reachable, it parks with a resume predicate and re-runs P0 inference with those gates excluded — it does NOT mark the mission blocked or complete.

## Progress

- 2026-07-19 — Mission opened. Grounded origin/main (`2d1f872`); proved gate end-to-end (clean→0, drift→1, restore→0, `--json` clean); fixed stale repo description + added drift topics. Collision preflight: 3 legacy claudux automations all stale/complete on old `leojkwan/claudux` remote — no live executor, no collision. PLAN landed via branch/PR (#86).
- 2026-07-19 — **Cycle 1: R2 ✅ + R4 ✅** (PR #87, merged → `49d4812`). Green gate on origin/main HEAD: shellcheck 0, `tests/run-all.sh` **314/314** (fixed 2 stale header tests — `head -2`→`head -4` after the #85 re-center made the header 3 lines), `npm pack --dry-run` = `claudux-1.2.0.tgz` (54 files). R2: `docs-drift-lock.json` re-baselined to include `lib/drift.sh`; `drift --json` now `ready=true, drifted=0, new_sources=0`. **Bonus fix (gate correctness):** `bin/claudux` routed the `📍 Changing to project root` notice to **stderr** — it was corrupting `--json` stdout, breaking every CI JSON consumer. Worktree landed + torn down; both branches pruned. Next P0: R1 (dogfood in own CI) or R3 (second real repo).
- 2026-07-19 — **Cycle 2: R3 ✅ + real bug fixed** (PR #89, merged → `88fc2a2`). Proved the gate on **resplit-web** (a real `significant`-mode repo, throwaway worktree, real repo left unpolluted — no lock committed): no-baseline→exit 0; `--accept` seed→exit 0; real `.env.example` token change with doc untouched→**exit 1** naming `docs/reference/configuration.md ← .env.example`; RED report prints the exact `--accept` fix hint. **This surfaced a real correctness bug the self-dogfood (surface mode) could never hit:** the `dir/**` glob matcher stripped `/**` (3 chars, incl. the slash), so `docs/**` matched the root file `docs-drift-lock.json` → the gate never converged on `--accept`. Fixed `slice(0,-3)`→`slice(0,-2)` in **both** matcher copies (`drift.sh`, `docs-manifest.sh`) per the determinism contract; added `test-drift.sh` Test 11 (verified it fails on old code, passes on fix). Full gate: shellcheck 0, **318/318**, npm pack OK. Worktrees torn down, branch pruned.
  - **R1 PARKED** (not blocked): the `docs-drift.yml` workflow already gates the repo (`bash bin/claudux drift` on every PR + push to main — confirmed in the tree), so the *plumbing* half of R1 is TRUE. The remaining half (capture a CI run URL + push a deliberate-drift PR red→green) is blocked on a **GitHub Actions API outage** (`gh run list` → HTTP 503, persistent across backoff). Wake condition: `gh run list --workflow=docs-drift.yml` returns non-503. Resume: capture the run URL from a recent PR (#87/#89 both triggered it) + push a deliberate-drift PR to watch it go red then green. Next reachable P0 while parked: R5/R6 (P2) or hold.
    - **⚠️ Correction (cycle 3): the cycle-2 diagnosis above was WRONG.** The 503 was scoped to ONE endpoint (`gh run list --workflow`); `gh pr checks`, `gh api .../check-runs`, and `gh api .../actions/runs` all worked the whole time. The real reason the gate never ran: every `docs-drift.yml` run sat `queued` because the workflow targeted the third-party `blacksmith-2vcpu-ubuntu-2404` runner, which was not picking up jobs. So the gate had *never actually executed* in claudux's own CI. (Tool-reach discipline: a "blocked" claim must name the tool tried + why it failed — the endpoint 503 was mistaken for a platform outage.)
    - **⚠️ Correction to the correction (same cycle, after checking run history): "a runner not servicing this repo" was ALSO wrong.** Blacksmith *does* service this repo and was working recently — `CI` completed **success on `main` 2026-07-18T18:16Z**, and `Docs (VitePress) to GitHub Pages` completed **success 2026-07-19T17:17Z**, both on Blacksmith. What is true: **no Blacksmith run has executed since 2026-07-19T17:17Z**, and every run queued from 2026-07-20T00:50Z onward is still waiting. That is a **current Blacksmith degradation/outage**, not a permanent misconfiguration. The README `ci.yml` badge is therefore *not* broken (last `main` run green). The #91 switch to `ubuntu-latest` remains the right call, but for the durable reason rather than the diagnostic one: **the gate that IS the product's credibility claim must not depend on third-party runner availability, and the copy-paste example promises vanilla `ubuntu-latest`.** Lesson: I asserted a root cause from one symptom (queued runs) without reading run history; the ref-and-receipts rule applies to diagnoses, not just audits.
    - **Handoff risk for the LEO-gated publish:** `publish.yml`, `ci.yml`, `docs.yml`, and `ci-testbox.yml` all still target Blacksmith. While Blacksmith is degraded, an `npm publish` triggered through `publish.yml` would sit queued rather than fail loudly. Before the 1.2.0 publish, either wait for Blacksmith to recover or move `publish.yml` to `ubuntu-latest`. Not fixed this cycle: changing the publish path is release-critical and should not ride along with unrelated work.
- 2026-07-20 — **Cycle 3: R1 ✅ COMPLETE** (own-CI dogfood proven end-to-end). Root-caused the parked R1 (see correction above) and fixed it: switched ONLY `docs-drift.yml` to `runs-on: ubuntu-latest` (the vanilla GitHub-hosted runner the copy-paste example promises — zero special infra is the whole credibility claim), PR **#91 merged → `840d318`**. Proof, all on the real surface (claudux's own Actions):
  - Gate now **executes** on ubuntu-latest — [run 29710603396](https://github.com/firstbitelabsllc/claudux/actions/runs/29710603396/job/88253813478), ✅ green, 12s (previously: never ran, stuck `queued` on Blacksmith).
  - **Red:** deliberate-drift demo PR #92 added `is_protected_secret` to `lib/content-protection.sh`, touched no doc → gate **FAILED**, [run 29710720980](https://github.com/firstbitelabsllc/claudux/actions/runs/29710720980/job/88254066087), 18s (named `lib/content-protection.sh` across 10 doc units, printed the exact `--accept` fix).
  - **Green:** `claudux drift --accept` re-baselined the lock → same gate **PASSED**, [run 29710745539](https://github.com/firstbitelabsllc/claudux/actions/runs/29710745539/job/88254128111). Red→green closed on one PR. #92 closed without merge (demo, not a feature); worktree + branch torn down.
  - **Mission terminal-condition status:** own-CI dogfood (R1) ✅ **AND** second real repo (R3, resplit-web) ✅ **AND** full green gate (R4, 318/318) ✅ — the credibility spine is closed. The launch question ("real deterministic lint or AI wrapper?") is answered on the real surface: keyless, offline, ~15s, LLM never on the pass/fail path, and claudux's own red X is on the record. Remaining rows are P2 polish (R5 pre-commit/README "Add to CI", R6 headless-render proof) + LEO-gated publish 1.2.0.

- 2026-07-20 — **Cycle 3 (cont.): R5 ✅ + R6 ✅ — all agent-completable rows now closed.**
  - **R5** (PR #95, merged): added the `--warn-only` pre-commit hook example the drift-gate section referenced by flag but never showed. Both snippets **copy-run verified, not assumed**: (a) CI snippet — `npm pack firstbitelabsllc/claudux` resolves the GitHub shorthand → `claudux-1.2.0.tgz` (54 files, `bin: claudux → ./bin/claudux`); installed that tarball to an isolated prefix and ran the bin as CI would → `claudux drift --json` in a fresh git repo returns exit 0 with `{"ready":true,"skipped":[{"reason":"no-baseline"}]}`. (b) pre-commit snippet — installed the hook in a clone of `origin/main` with REAL drift present; the commit printed the full report naming `lib/content-protection.sh` + the fix and **still landed** (`c843428`), which is exactly what `--warn-only` promises.
  - **R6** (headless Playwright CLI, light + dark, 1280×1400 @2x): live GitHub README renders correctly in **both** schemes. `terminal-demo.svg` **is served and painted** (`/raw/main/assets/terminal-demo.svg`, naturalWidth 300×120, scaled by `width="100%"`) — **no GitHub `<img>` sanitizer problem**, which was R6's named risk. Comparison table renders (8 rows, staleness row present, 417px). Drift-verdict headline, "The drift gate" section, and the new `--warn-only` content all present. Zero broken images in either scheme. Screenshots read back visually, not just asserted from the DOM. One false alarm checked and cleared: the `--warn-only` code span *looked* like an em dash at downscaled resolution; DOM confirms `<code>--warn-only</code>` with two genuine `U+002D` hyphens, so GitHub is not applying smart punctuation.
  - **Side effect I caused and reverted:** while installing the test hook, `git rev-parse --git-path hooks/pre-commit` run **from a worktree** resolved to the MAIN checkout (in a worktree `.git` is a file, not a directory), so a hook was briefly written into `/Users/leokwan/Development/claudux/.git/hooks/`. Verified nothing was clobbered (dir held only `.sample` files; no `core.hooksPath`, no husky) and removed it — main checkout left exactly as found. The real hook test was then run in an isolated clone. Lesson: never write to `.git/` paths from a worktree without checking where they resolve.
  - **Row status: R1–R6 all ✅.** The only remaining mission element is the LEO-GATED publish, parked below as a handoff — NOT a stop, NOT blocked.

- 2026-07-20 — **Cycle 4: R7 (unplanned P0) ✅ — the public docs site was still selling the dead category.** With R1–R6 closed and publish LEO-gated, P0 inference over the remaining launch surface found the largest reachable credibility break was *outside* the row table: https://firstbitelabsllc.github.io/claudux/ served `AI-powered documentation generator for your codebase using Claude Code and VitePress` and its landing page never mentioned `claudux drift` at all. Anyone following the re-centered README landed on a page selling the pre-1.2.0 product. **PR #97 merged → `427e54a`.**
  - Re-centered `docs/index.md` (hero, tagline, six features, quick start with the same honest npm-serves-1.1.1 caveat as the README, commands table now lists `drift` + its four flags) and `docs/.vitepress/config.ts` (meta description + og:title).
  - Replaced the doc-debt filler section with **Why this exists** (docs' primary reader is shifting from human to agent) and **How the gate works** (baseline → compare → exit 0/1/2).
  - Moved the Pages deploy off `blacksmith-2vcpu-ubuntu-2404` to `ubuntu-latest`, same durable reason as #91: the launch-facing surface must not depend on third-party runner availability.
  - **Proof on the real surface:** `npm ci && DOCS_BASE=/claudux/ npx vitepress build` clean (2.36s); built `index.html` carried the new meta/hero; `bash bin/claudux drift` exit 0; PR check `doc/code drift gate` **pass 10s**; docs deploy run `29711222707` **success in ~60s on ubuntu-latest** (the first successful docs deploy since 2026-07-19T17:17Z); live site re-fetched and confirmed serving the new description and hero.
  - **Diagnosis honesty:** the `cancelled` docs runs were `cancel-in-progress: true` concurrency cancellations, NOT outage evidence. Only the queued-with-no-runner job supports the Blacksmith-degraded read, and the runner change did not depend on that diagnosis. Left `ci.yml`, `ci-testbox.yml`, and `publish.yml` on Blacksmith — not launch-visible, and Leo's publish path is local.
  - **Row status: R1–R6 ✅, R7 ✅.** Only the LEO-GATED publish remains. Mission state WAITING, not blocked, not complete.

- 2026-07-20 — **Cycle 5: R8 (unplanned P0) ✅ — the re-center was skin-deep behind the landing page.** After R7 fixed the docs home, an audit of every docs page found `drift` mentioned **0 times across all 16 interior pages**. The hero promised a CI gate; `Get Started`, the command reference, and the features overview all still described a doc generator. That is the same lie the product exists to catch. **PR #99 merged → `abe0c92`.**
  - New `docs/features/drift-gate.md`, written against `lib/drift.sh` rather than from memory: declare → baseline → compare → exit, the three sensitivity modes with the residual false positives stated honestly, re-baselining, the copy-paste CI workflow, the `--warn-only` pre-commit hook, the `--json` shape, and the no-lock / delete / rename edges.
  - `guide/commands.md`: `claudux drift` promoted to its own section ahead of the core commands, with a flags table and drift-specific exit-code meaning (`1` = drift found, `2` = could-not-evaluate).
  - `guide/index.md` + `features/index.md`: lead with the two things claudux does; gate listed first; same honest npm-serves-1.1.1 install caveat as the README.
  - `config.ts`: "The Drift Gate" added to both the guide and features sidebars.
  - **Proof on the real surface:** `npm ci && DOCS_BASE=/claudux/ npx vitepress build` clean in 2.50s — VitePress fails the build on dead links, so this validates every new cross-link; `features/drift-gate.html` present in `dist/`; `bash bin/claudux drift` exit 0; `tests/run-all.sh` all suites passed; PR check `doc/code drift gate` **pass 10s**; live page confirmed at https://firstbitelabsllc.github.io/claudux/features/drift-gate.
  - **Row status: R1–R6 ✅, R7 ✅, R8 ✅.** Only the LEO-GATED publish remains. Mission state WAITING, not blocked, not complete.

- 2026-07-20 — **Cycle 6: R9 (unplanned P0) ✅ — Dependabot cleared, 4 open alerts → 0.** A repo about to be launched publicly was showing `1 high, 3 moderate` on its security tab. All four were transitive `vite`/`esbuild` under vitepress in `docs/package-lock.json`. **PR #101 merged → `8027b1c`.**
  - **No easy fix existed, and I checked before assuming one did:** `5.4.21` is the last 5.x, every advisory is patched only in `vite 6.4.3` / `esbuild 0.25.0`, vitepress `1.6.4` (latest 1.x) pins `vite ^5`, and vitepress 2 is alpha-only on `vite ^8` — not an option for a launch repo. An npm `overrides` block was the remaining route, and I tested it against the real build rather than shipping it on hope.
  - **Proof on the real surface:** `npm ci` from the new lock succeeds (that is the CI path, not just `npm install`); vite resolves to `6.4.3`, esbuild to `0.25.12`; `DOCS_BASE=/claudux/ npx vitepress build` clean in 2.75s with every guide and features page present in `dist/`; `npx vitepress dev` boots and serves 200; `npm audit` → **found 0 vulnerabilities**; PR check `doc/code drift gate` **pass 8s**; post-merge Pages deploy run `29711448236` **success**; `gh api .../dependabot/alerts` open count → **0**.
  - **Severity honesty:** all three advisories are dev-server issues (`server.fs.deny` bypass, esbuild dev-server CORS, optimized-deps path traversal). They never applied to the static Pages output or to the drift gate, which runs no server at all. The fix was worth doing for the public security tab, not because the gate was exposed.
  - **Row status: R1–R6 ✅, R7 ✅, R8 ✅, R9 ✅.** Only the LEO-GATED publish remains. Mission state WAITING, not blocked, not complete.

- 2026-07-20 — **Cycle 6 (cont.): COLLISION FOUND — open PR #84 is CONFLICTING, and I caused part of it. Raising, not fixing.**
  - PR **#84** (`fix/publish-readiness-audit`, author `leojkwan`, open since 2026-07-19, +330/-195 across 22 files) is a pre-publish readiness audit fixing **23 stranger-facing issues**. It is `mergeable=CONFLICTING`, `mergeStateStatus=DIRTY`.
  - **My cycles 4-6 wrote into five files that PR also touches**: `docs/index.md` (#97), `docs/guide/commands.md` (#99), `docs/package.json` + `docs/package-lock.json` (#101), and `docs-drift-lock.json`. This is an **Own-Row Guard violation** — I checked the PLAN row table before editing but never the open-PR list. The PLAN is not the only claim surface; open PRs are.
  - **PR #84 carries launch blockers this mission did not have**, and they outrank everything I shipped in cycles 4-6: the README described the tool as **1.1.1 while `package.json` says 1.2.0**, and npm freezes the README into the version page permanently — publishing with that text ships a page that cannot be corrected without burning a version number. It also fixes `claudux --version`/`--help` erroring outside a git repo, `serve` demanding an AI CLI to run a static server, install docs naming the nonexistent `@anthropic-ai/claude-cli`, a `lib/cleanup.sh` call to a `claude api` subcommand that does not exist, a `main` field pointing at a missing file, and tarball weight (432.9 kB -> **87.7 kB**).
  - **One #84 blocker is resolved by cycle-6 work.** #84 states the three vite advisories are "unpatched across the entire 5.x and 6.x lines and fixed only in vite 7, which requires Node 20.19+ and so collides with this package's declared `engines.node >=18`." That is not correct as of today: **`vite@6.4.3` declares `engines.node: ^18.0.0 || ^20.0.0 || >=22.0.0`** — Node 18 is supported — and after #101 the repo's open Dependabot count is **0** (`gh api .../dependabot/alerts`). The docs build and Pages deploy both ran green on CI's `node-version: 18`. The Node-18 collision #84 describes as unresolved does not exist on vite 6.4.3.
  - **Not taken, deliberately:** I did not rebase, force-push, retarget, or close #84. It is not my branch and rewriting it is a destructive op on user-staged work. Resolution is Leo's call.
  - **Process failure to fix, mine:** I also closed my own PR #103 by deleting its branch before verifying the squash-merge had landed. Teardown must be gated on a confirmed `MERGED` state, never chained onto the merge command.
  - **Mission state WAITING** — unchanged, still on the LEO-GATED publish, now with a second Leo decision stacked in front of it (how to land #84).

- 2026-07-20 — **Cycle 7: the P0 was real and it was a blind gate. Own-CI dogfood now landed on a mode that can see.**
  - **R10 — RETRACTED, see cycle 8. The claim below was false; claudux was already dogfooding via `docs-drift.yml` since #91.** ~~claudux did not run its own gate.~~ `.github/workflows/ci.yml` had 7 jobs and none of them was `claudux drift`. A tool whose pitch is "CI-enforced doc/code lint" was not CI-enforcing itself. Landed via **#105 -> `e9b0079`**.
  - **The finding that reordered the cycle:** this repo dogfooded in `drift_sensitivity: surface`, which hashes only extractable symbols. Files with no symbol surface all hash to the same constant, so **35 of 54 declared sources shared one hash and could never drift** — `README.md`, `package.json`, `docs-structure.json`, every template, the entire VitePress theme. Proven, not inferred: appending a fabricated "claudux now mines cryptocurrency" section to `README.md` left the gate printing `Docs match code`, **exit 0**.
  - Wiring own-CI on top of that would have shipped a green badge backed by a check blind to two-thirds of its inputs — the precise thing a skeptic asking "real lint or AI wrapper?" finds first. So the fix landed with the job: switch this repo to `significant` (**54/54 unique hashes**), where the same `README.md` edit **exits 1** and names both affected pages (`docs/index.md`, `docs/troubleshooting.md`). No change to `surface` semantics; only this repo's config moved.
  - **Verdict on the launch question: it is a real deterministic lint.** Measured on a clean `origin/main` worktree with exit codes read via `$?` on the command itself (an earlier reading was invalid — `$?` after a pipe captures `tail`, not `claudux`): drift -> **exit 1**, clean -> **exit 0**, `--json` well-formed (`ready:true`). Runs keyless and offline. Real CI proof: **run 29711688205, `doc/code drift gate` pass in 9s**.
  - **R11 — the gate missed a live lie, and it was mine.** `docs/features/drift-gate.md` said "claudux dogfoods itself in this mode" about `surface`; `e9b0079` made that false and the gate did not flag it, so the launch-facing docs site served an incorrect claim about its own code. Corrected and turned into an honest disclosure section, **What `surface` mode cannot see**, carrying the 35-of-54 numbers. **#106 -> `ca585c8`**, drift check pass 8s (run 29712008935), `vitepress build` clean 2.31s.
  - **Coverage gap worth a future row:** a doc sentence describing a config value was not caught when that config value changed. Page-level `source_patterns` coverage does not reach claims about `docs-structure.json` contents. Not a correctness bug in the comparator; a granularity limit, and the same one the deferred symbol/line-anchor tier addresses.
  - **Blacksmith note — corrected.** I merged #105 stating `Local CI pointer` (workflow "Blacksmith Testbox") "sits `queued` indefinitely" on a third-party runner. That was wrong: a delayed poller showed it picked up a runner and **passed in 4s** (run 29711688219). The merge outcome was fine — every check including that one went green — but the stated reason was not. Blacksmith runner acquisition here is slow, not absent; do not cite it as an outage without waiting the job out. The durable reason for moving the Pages deploy to `ubuntu-latest` stands on its own (a launch-facing surface should not depend on third-party runner availability) and does not rest on this.
  - **Process fix applied this cycle:** teardown is now gated on a verified `MERGED` + a `git show origin/main` content check, never chained onto the merge command. Also: two verification batches silently no-opped by running `git` from a non-repo cwd, which briefly looked like a failed merge. State the ref, and confirm the command actually ran in the repo.

- 2026-07-20 — **Cycle 8: R10 was wrong. Retracted. The real CI hole was worse, and is now fixed.**
  - **Retraction.** Cycle 7 claimed "claudux shipped a CI gate and did not run it in its own CI." False. `.github/workflows/docs-drift.yml` has existed since **`840d318` (#91, 2026-07-19)** — a dedicated drift workflow on `ubuntu-latest`, with copy-paste adoption docs in its header. The `doc/code drift gate` check green on every PR is that workflow, not the job #105 added. Root cause: I grepped `.github/workflows/` on a **stale local checkout (`8b0ea6a`)** that predates `docs-drift.yml` — the same stale-ref error as earlier in the session, made twice, the second time load-bearing enough to build a PR and a PLAN entry on. The `surface`->`significant` blind-spot fix in #105 was independently real and stands; only the framing was wrong.
  - The job #105 added to `ci.yml` was both redundant and **dead** — `ci.yml` triggered only on `workflow_dispatch`/`workflow_call`, so it never fired on a PR. Removed in #110.
  - **R12 — the real hole (found while chasing the retraction).** `ci.yml` carries shellcheck, structure, syntax, version-consistency, the full test suite, docs-build and release-readiness. Its only caller, `ci-testbox.yml`, triggers on `pull_request` restricted to `paths: ['.github/workflows/**']`. Net: **a PR touching only `bin/claudux` or `lib/*.sh` received no lint and no tests.** Demonstrated on #109, which rewrites `bin/claudux` and five lib files immediately before a public npm publish and whose entire check list was one drift job. Fixed by running `ci.yml` on push/PR to main — **#110 -> `2f9025e`**, after which a PR shows all 7 jobs green.
  - **R13 — #84 landed.** @leojkwan's publish-readiness audit (23 stranger-facing fixes) merged cleanly into current main; its `CONFLICTING` status was stale. Landed as a **separate branch, #84 left untouched** — no force-push over another author's work. **#109 -> `f2e35c9`**, now with real coverage: shellcheck, bash -n, structure, version, full test suite, docs build, release readiness, drift — all pass. `npm pack` **98.0 kB / 53 files** (was 432.9 kB). Fixes `--version`/`--help` outside a git repo, `serve` demanding an AI CLI, the nonexistent `@anthropic-ai/claude-cli` install docs, a `claude api` call that does not exist, a `main` field pointing at a missing file, `claude.md` -> `CLAUDE.md`.
  - **Process:** twice this session a `git` batch silently no-opped from a non-repo cwd, and once a `git push -f` was chained onto a mid-conflict rebase (no damage — remote matched the clean pre-rebase commit — but the chaining was the same mistake class that closed #103). Rebase was also the wrong tool: it replays #84's obsolete vite-7 commit against the vite-6.4.3 fix from #101. Merge resolves final states and was clean both times.
  - **Mission state: WAITING on `npm login` only.** Every agent-reachable pre-publish item is closed. Repo is 1.2.0, registry serves 1.1.1.

- 2026-07-20 — **Cycle 9: README sequencing corrected, and the published artifact itself proven.**
  - **Sequencing error, self-caught.** Cycle 8's handoff said the "install from source until 1.2.0 is on npm" caveat should be removed *after* publishing. That is backwards. **npm freezes the README onto the version page at publish time**, so publishing with that caveat would permanently serve a page telling strangers the thing they are looking at does not exist yet. Stripped from `README.md` (3 sites), `docs/index.md`, `docs/guide/index.md`; the CI copy-paste block is now a single `- run: npx claudux drift`. **#112 -> `903b777`**, all 10 checks green. Verified landed with `git show origin/main:README.md`.
  - The drift gate **caught this edit on itself** — `claudux drift` exited 1 naming 5 pages that document `README.md`. Reviewed each (`docs/guide/installation.md`'s git-clone block and `docs/troubleshooting.md`'s issues URL are both still correct), then re-baselined with `--accept`. Dogfooding worked as designed.
  - **R14 — the published artifact is proven, not just the repo.** Prior cycles proved `./bin/claudux` from a checkout. This cycle installed the actual packed tarball (`npm i -g <tgz>` into a clean prefix) and ran the installed binary:
    - `claudux --version` -> `claudux 1.2.0`
    - clean tree -> **exit 0**, `--json` `ready:true`, `sensitivity:significant`, `drifted:0`
    - append a real function to `lib/project.sh`, touch no docs -> **exit 1**, `ready:false`, 6 doc units named with `changed_sources:["lib/project.sh"]` and the `--accept` remedy printed
    - `git checkout` the source -> **exit 0** again (deterministic, no residue)
    - No API key, no network. This is the artifact strangers will install.
  - **Pre-publish audit workflow discarded.** The 5-dimension adversarial tarball audit (`wf_c2acb5f6-121`) died incomplete — all five agents returned `null`, and its transcripts show it was reading the pre-fix README, so its inputs were stale regardless. Not cited as evidence. The direct install smoke test above replaces it and is stronger.
  - **R15 — `vidux` codename ships publicly. Deliberately deferred, not missed.** `lib/templates/vidux-project-config.json` is in the tarball and "Vidux Team Agents" is a nav entry on the public docs site. This is **not** stray cruft: `lib/project.sh:34-36` detects `vidux` as a first-class project type and `tests/run-tests.sh:363-414` asserts it. Removing it is a behavior change with test surface, and doing that under publish pressure is how launches break. Call: **ships in 1.2.0, cleaned in 1.2.1** (docs nav + template rename are cheap and reversible; the npm README does not mention it).

### LEO handoff — publish 1.2.0 (one command, needs your 2FA)

Everything agent-side is closed. Receipts as of `origin/main` = **`903b777`**:

- Registry serves **1.1.1**; repo is **1.2.0**. The README on `origin/main` is now publish-correct — the source-install caveat is **gone**, so the frozen 1.2.0 version page will be right.
- Staged and verified: `/tmp/wt-pub` is a clean checkout of `903b777` with `claudux-1.2.0.tgz` packed (**98.0 kB, 53 files**, down from 432.9 kB).
- `npm whoami` -> **`leojkwan`**. Login is done. The only remaining gate is the **2FA OTP**, which an agent must never type.

```bash
cd /tmp/wt-pub && npm publish --otp=YOUR_6_DIGIT_CODE
```

**Do not publish via CI.** `publish.yml` targets the Blacksmith runner; a CI-triggered publish sits `queued` silently rather than failing loudly. Publish locally with the command above.

**After it lands:** `npx claudux drift` becomes the real quickstart, and the CI copy-paste block already in the README starts working for strangers.

## Fleet coordination

Single owner per cycle. Fresh-read this PLAN + live claims + `origin/main` before write ownership; claim one unowned row; park conflicts as `waiting_on_owner` and rerank. Checkpoint only on material claim-state change. This is the ONLY executor for this mission; the `cost-100x` provider-control goal is a SEPARATE mission with its own PLAN — do not merge or collide.
